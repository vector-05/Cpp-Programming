#include <iostream>
#include <cmath>

int main() {

    // math in cpp

    int n1 = 2, n2 = 4;
    std::cout << std::max(n1, n2) << std::endl << std::min(n1, n2) << std::endl;

    std::cout << sqrt(64) << std::endl << round(19.98) << std::endl << log(34) << std::endl;

    return 0;
}